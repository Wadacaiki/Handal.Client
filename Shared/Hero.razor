<section class="py-24 lg:py-32">
  <div class="max-w-5xl">
    <!-- Бейдж -->
    <div class="inline-flex items-center gap-2 px-5 py-2 rounded-full mb-8 backdrop-blur-xl"
         style="background: linear-gradient(135deg, rgba(239, 68, 68, 0.2) 0%, rgba(239, 68, 68, 0.05) 100%); border: 1px solid rgba(239, 68, 68, 0.3)">
      <span class="w-4 h-4 animate-pulse">⚡</span>
      <span class="text-sm font-bold" style="color: #fbbf24">ДОБРО ПОЖАЛОВАТЬ НА HANDAL</span>
    </div>

    <!-- Заголовок -->
    <h1 class="text-6xl md:text-7xl lg:text-8xl font-black m-0 mb-6 leading-tight text-white" 
        style="letter-spacing: -0.02em">
      Торгуйте <span class="relative inline-block" style="background: linear-gradient(135deg, #ef4444 0%, #f97316 100%); background-clip: text; -webkit-background-clip: text; -webkit-text-fill-color: transparent">премиум
        <span class="absolute -bottom-3 left-0 right-0 h-3 rounded-full block" style="background-color: #ef4444; opacity: 0.3; filter: blur(8px)"></span>
      </span> <br />предметами
    </h1>

    <!-- Описание -->
    <p class="text-xl lg:text-2xl max-w-3xl mb-12 leading-relaxed text-slate-300">
      Присоединяйтесь к эксклюзивной платформе для торговли редкими и ценными предметами. <span class="text-amber-400 font-semibold">Безопасность, прозрачность, экспертиза.</span>
    </p>

    <!-- Кнопки CTA -->
    <div class="flex flex-col sm:flex-row gap-4 mb-16">
      <button class="group px-10 py-5 rounded-2xl font-bold text-lg flex items-center justify-center gap-3 relative overflow-hidden text-white"
              style="background: linear-gradient(135deg, #ef4444 0%, #dc2626 100%); box-shadow: 0 30px 80px rgba(239, 68, 68, 0.3); border: 2px solid rgba(239, 68, 68, 0.5)">
        Начать торговлю
        <span class="w-6 h-6 group-hover:translate-x-1 transition-transform"></span>
      </button>

      <button class="px-10 py-5 rounded-2xl backdrop-blur-xl font-bold text-lg flex items-center justify-center gap-2 text-white"
              style="background-color: rgba(255, 255, 255, 0.05); border: 2px solid rgba(255, 255, 255, 0.1); box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1)">
        Узнать больше
      </button>
    </div>

    <!-- Статистика -->
    <div class="grid grid-cols-3 gap-6 md:gap-12 pt-12 border-t" style="border-top-color: rgba(100, 116, 139, 0.2)">
      <div class="group cursor-pointer">
        <div class="text-4xl md:text-5xl font-black m-0 mb-3 bg-gradient-to-r from-amber-300 to-yellow-300 bg-clip-text text-transparent group-hover:from-amber-200 group-hover:to-yellow-200 transition-all">50K+</div>
        <p class="text-sm m-0 text-slate-400 group-hover:text-slate-300 transition-colors">Активных пользователей</p>
      </div>
      <div class="group cursor-pointer">
        <div class="text-4xl md:text-5xl font-black m-0 mb-3 bg-gradient-to-r from-amber-400 to-yellow-400 bg-clip-text text-transparent group-hover:from-amber-300 group-hover:to-yellow-300 transition-all">10K+</div>
        <p class="text-sm m-0 text-slate-400 group-hover:text-slate-300 transition-colors">Завершено аукционов</p>
      </div>
      <div class="group cursor-pointer">
        <div class="text-4xl md:text-5xl font-black m-0 mb-3 bg-gradient-to-r from-green-400 to-emerald-400 bg-clip-text text-transparent group-hover:from-green-300 group-hover:to-emerald-300 transition-all">$1B+</div>
        <p class="text-sm m-0 text-slate-400 group-hover:text-slate-300 transition-colors">Общий объем торговли</p>
      </div>
    </div>
  </div>
</section>